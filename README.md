# 小学成绩管理系统

一个现代化的小学成绩管理系统，采用前后端分离架构，部署在 Cloudflare 平台。

## ✨ 功能特性

### 核心功能
- 📊 **成绩管理** - 录入、查询、导入、导出学生成绩
- 👥 **学生管理** - 学生信息维护，支持批量导入
- 📚 **班级管理** - 班级和科目信息管理
- 📝 **考试管理** - 考试安排和成绩统计

### 数据分析
- 📈 **仪表盘** - 实时数据概览和可视化图表
- 📉 **班级分析** - 成绩趋势、学科对比、年级排名
- 🎯 **重点关注** - 自动识别需要关注的学生群体
- 👤 **学生档案** - 个人成绩历史和学科雷达图

### 系统功能
- 🔐 **用户认证** - 安全的登录和权限管理
- 📄 **操作日志** - 完整的审计日志记录
- 📥 **数据导入导出** - Excel 批量操作支持

## 🛠️ 技术栈

### 前端
- **框架**: React 19 + TypeScript
- **构建工具**: Vite 6
- **UI 组件**: Ant Design
- **状态管理**: Zustand
- **图表**: Recharts
- **路由**: React Router v6

### 后端
- **框架**: Hono (Cloudflare Workers)
- **数据库**: Cloudflare D1 (SQLite)
- **认证**: JWT
- **文件处理**: XLSX (Excel 导入导出)

### 部署
- **前端**: Cloudflare Pages
- **后端**: Cloudflare Workers
- **CI/CD**: GitHub Actions

## 🚀 快速开始

### 环境要求
- Node.js 20+
- npm 或 pnpm

### 本地开发

#### 1. 克隆项目
```bash
git clone https://github.com/Misaka450/bao-class.git
cd bao-class
```

#### 2. 启动后端
```bash
cd api
npm install
npm run dev
```
后端将运行在 `http://localhost:8787`

#### 3. 启动前端
```bash
cd frontend
npm install
npm run dev
```
前端将运行在 `http://localhost:5173`

#### 4. 初始化数据
访问 `http://localhost:8787/api/init/all` 初始化数据库和测试数据

默认登录账号：
- 用户名: `admin`
- 密码: `admin123`

## 📦 部署

### 自动部署（推荐）

本项目已配置 GitHub Actions，推送到 `master` 分支会自动部署：

1. **配置 GitHub Secrets**
   - `CLOUDFLARE_API_TOKEN` - Cloudflare API Token
   - `CLOUDFLARE_ACCOUNT_ID` - Cloudflare Account ID
   
   详细步骤见 [GITHUB_ACTIONS.md](./GITHUB_ACTIONS.md)

2. **推送代码**
   ```bash
   git push origin master
   ```

3. **查看部署状态**
   - GitHub Actions 会自动部署前端和后端
   -

### 手动部署

#### 部署后端
```bash
cd api
npm run deploy
```

#### 部署前端
```bash
cd frontend
npm run build
npx wrangler pages deploy dist --project-name=bao-class
```

## 📚 项目结构

```
bao-class/
├── api/                    # 后端代码
│   ├── src/
│   │   ├── routes/        # API 路由
│   │   ├── middleware/    # 中间件
│   │   └── utils/         # 工具函数
│   └── wrangler.toml      # Cloudflare Workers 配置
├── frontend/              # 前端代码
│   ├── src/
│   │   ├── pages/        # 页面组件
│   │   ├── components/   # 公共组件
│   │   ├── services/     # API 服务层
│   │   ├── types/        # TypeScript 类型定义
│   │   └── store/        # 状态管理
│   └── vite.config.ts    # Vite 配置
└── .github/
    └── workflows/         # GitHub Actions 配置
```

## � 文档

- [API 文档](./API_DOCS.md) - 后端 API 接口说明
- [GitHub Actions 配置](./GITHUB_ACTIONS.md) - 自动部署配置指南
- [项目文档](./项目文档.md) - 详细的项目需求和设计文档

## 🔧 开发指南

### 代码规范
- 使用 Prettier 格式化代码
- TypeScript 严格模式
- 统一的错误处理和日志记录

### API 架构
- 前端使用统一的 API 服务层 (`src/services/api.ts`)
- 所有请求自动处理认证和错误
- 类型安全的接口定义

### 数据库
- 使用 D1 (SQLite)
- Schema 定义在 `api/schema.sql`
- 支持本地和云端数据库

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可

MIT License

